package currency.converter;

import org.junit.jupiter.api.Test;

import java.util.*;

import static org.junit.jupiter.api.Assertions.*;

public class CalculatorTests {

    // @Test
    // void testCalcStats() {
    // CurrencyCalculator calculator = new CurrencyCalculator();
    // List<Float> list = Arrays.asList(14f, 2f, 4f, 1f, 5f, 4f);

    // Map<String, Float> result = calculator.calculateStatistic(list);

    // assertEquals(result.get("average"), 5f);
    // assertEquals(result.get("median"), 4f);
    // assertEquals(result.get("max"), 14f);
    // assertEquals(result.get("min"), 1f);
    // assertTrue((4.241 < result.get("sd")) && (4.243 > result.get("sd")));
    // }

    // @Test
    // void testCalcAmount() {
    // CurrencyCalculator calculator = new CurrencyCalculator();

    // assertEquals(calculator.calculateAmount(132, (float) 0.4), 52.8);
    // }
}
